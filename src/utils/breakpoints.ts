/**
 * Below this width, the homepage timeline collapses to one column.
 * Matches the 900px breakpoint documented in src/layouts/base.css.
 */
export const timelineCollapsedQuery = "(width < 900px)";
