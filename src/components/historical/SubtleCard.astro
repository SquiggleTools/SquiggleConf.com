---
interface Props {
	class?: string;
	as?: "div" | "li";
}

const { as: As = "div", class: className, ...rest } = Astro.props;
---

<As class:list={["subtle-card", className]} {...rest}>
	<slot />
</As>

<style>
	.subtle-card {
		--cardPadding: 1rem;

		align-items: flex-start;
		background-color: var(--colorBackdropLight);
		border-radius: var(--radiusMedium);
		border: 1px solid var(--colorBackdropOff);
		box-shadow: 0 0 0.125rem var(--colorTextShadow);
		font-family: var(--fontFamilyBody);
		list-style: none;
		padding: var(--cardPadding);
	}
</style>
