<div class="deep-sea-surface">
	<slot />
</div>

<style>
	.deep-sea-surface {
		background: linear-gradient(
				to bottom,
				light-dark(#f8fcff, #004077),
				light-dark(#effaff, #013a6c),
				light-dark(#e6f7fe, var(--colorBlue4)),
				light-dark(#d8f3ff, #042446),
				light-dark(#c8edfb, #042140),
				light-dark(#c9effd, #041b37),
				light-dark(#c8efff, var(--colorBlue5)),
				light-dark(#aedff5, var(--colorBlue6))
			)
			var(--colorBlueBright2);
		color: var(--colorForegroundEmphasized);
		isolation: isolate;
		overflow: clip;
		position: relative;
	}

	.deep-sea-surface::after {
		background:
			radial-gradient(
				circle at top,
				light-dark(rgb(201 226 252 / 49%), transparent) 50%,
				light-dark(rgb(245 245 255 / 70%), transparent) 70%
			),
			repeating-linear-gradient(
				116deg,
				light-dark(rgb(4 25 48 / 5%), rgb(255 255 255 / 4.2%)) 0 2px,
				transparent 2px 18px,
				light-dark(rgb(17 109 154 / 10%), rgb(118 217 255 / 2.8%)) 18px 20px,
				transparent 20px 34px
			),
			repeating-linear-gradient(
				-64deg,
				light-dark(rgb(4 25 48 / 3%), rgb(255 255 255 / 1.8%)) 0 1px,
				transparent 1px 15px,
				light-dark(rgb(31 127 171 / 4%), rgb(56 173 223 / 2.6%)) 15px 17px,
				transparent 17px 31px
			);
		content: "";
		inset: 0;
		mix-blend-mode: multiply;
		opacity: 0.58;
		pointer-events: none;
		position: absolute;
		z-index: 0;
	}

	html.dark .deep-sea-surface::after {
		mix-blend-mode: screen;
		opacity: 0.31;
	}
</style>
