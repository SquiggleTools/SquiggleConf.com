<div class="deep-sea-surface">
	<slot />
</div>

<style>
	.deep-sea-surface {
		background: linear-gradient(
				to bottom,
				light-dark(#f8fcff, #004077),
				light-dark(#effaff, #013a6c),
				light-dark(#e6f7fe, var(--colorBlue4)),
				light-dark(#d8f3ff, #042446),
				light-dark(#c8edfb, #042140),
				light-dark(#c9effd, #041b37),
				light-dark(#c8efff, var(--colorBlue5)),
				light-dark(#aedff5, var(--colorBlue6))
			)
			var(--colorBlueBright2);
		color: var(--colorForegroundEmphasized);
		isolation: isolate;
		overflow: clip;
		position: relative;
	}

	.deep-sea-surface::after {
		background:
			radial-gradient(
				circle at top,
				light-dark(rgba(201, 226, 252, 0.49), transparent) 50%,
				light-dark(rgba(245, 245, 255, 0.7), transparent) 70%
			),
			repeating-linear-gradient(
				116deg,
				light-dark(rgba(4, 25, 48, 0.05), rgba(255, 255, 255, 0.042)) 0 2px,
				transparent 2px 18px,
				light-dark(rgba(17, 109, 154, 0.1), rgba(118, 217, 255, 0.028)) 18px
					20px,
				transparent 20px 34px
			),
			repeating-linear-gradient(
				-64deg,
				light-dark(rgba(4, 25, 48, 0.03), rgba(255, 255, 255, 0.018)) 0 1px,
				transparent 1px 15px,
				light-dark(rgba(31, 127, 171, 0.04), rgba(56, 173, 223, 0.026)) 15px
					17px,
				transparent 17px 31px
			);
		content: "";
		inset: 0;
		mix-blend-mode: multiply;
		opacity: 0.58;
		pointer-events: none;
		position: absolute;
		z-index: 0;
	}

	html.dark .deep-sea-surface::after {
		mix-blend-mode: screen;
		opacity: 0.31;
	}
</style>
