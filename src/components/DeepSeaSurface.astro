<div class="deep-sea-surface">
	<slot />
</div>

<style>
	.deep-sea-surface {
		background: linear-gradient(
				to bottom,
				var(--colorDeepSea1),
				var(--colorDeepSea2),
				var(--colorDeepSea3),
				var(--colorDeepSea4),
				var(--colorDeepSea5),
				var(--colorDeepSea6),
				var(--colorDeepSea7),
				var(--colorDeepSea8)
			)
			var(--colorBlueBright2);
		color: var(--colorForegroundEmphasized);
		isolation: isolate;
		overflow: clip;
		position: relative;
	}

	.deep-sea-surface::after {
		background:
			radial-gradient(
				circle at top,
				light-dark(rgb(from var(--colorDeepSeaGlow) r g b / 49%), transparent)
					50%,
				light-dark(rgb(from var(--colorDeepSeaHaze) r g b / 70%), transparent)
					70%
			),
			repeating-linear-gradient(
				116deg,
				light-dark(
						rgb(from var(--colorBlue5) r g b / 5%),
						rgb(from var(--colorWhite) r g b / 4.2%)
					)
					0 2px,
				transparent 2px 18px,
				light-dark(
						rgb(from var(--colorBlue3) r g b / 10%),
						rgb(from var(--colorDeepSeaStripeBright) r g b / 2.8%)
					)
					18px 20px,
				transparent 20px 34px
			),
			repeating-linear-gradient(
				-64deg,
				light-dark(
						rgb(from var(--colorBlue5) r g b / 3%),
						rgb(from var(--colorWhite) r g b / 1.8%)
					)
					0 1px,
				transparent 1px 15px,
				light-dark(
						rgb(from var(--colorOcean) r g b / 4%),
						rgb(from var(--colorDeepSeaStripeMedium) r g b / 2.6%)
					)
					15px 17px,
				transparent 17px 31px
			);
		content: "";
		inset: 0;
		mix-blend-mode: multiply;
		opacity: 0.58;
		pointer-events: none;
		position: absolute;
		z-index: 0;
	}

	html.dark .deep-sea-surface::after {
		mix-blend-mode: screen;
		opacity: 0.31;
	}
</style>
