<div class="deep-sea-surface">
	<slot />
</div>

<style>
	.deep-sea-surface {
		background: linear-gradient(
				to bottom,
				#f8fcff,
				#effaff,
				#e6f7fe,
				#d8f3ff,
				#c8edfb,
				#b9e7f8,
				#a8dff5,
				#98d8f3
			)
			var(--colorBlueBright2);
		color: var(--colorForegroundEmphasized);
		isolation: isolate;
		overflow: clip;
		position: relative;
	}

	:global(html.dark) .deep-sea-surface {
		background: linear-gradient(
				to bottom,
				#004077,
				#013a6c,
				#033461,
				#032e56,
				#04284c,
				#042446,
				#042140,
				#041d3a,
				#041b37,
				#041933,
				#041630,
				#04142d
			)
			var(--colorBlueBright3);
		color: var(--colorForegroundEmphasized);
	}

	.deep-sea-surface::after {
		background:
			radial-gradient(
				circle at top,
				rgba(200, 220, 240, 0.49) 50%,
				rgba(245, 245, 255, 0.7) 70%
			),
			repeating-linear-gradient(
				116deg,
				rgba(4, 25, 48, 0.12) 0 2px,
				transparent 2px 18px,
				rgba(17, 109, 154, 0.1) 18px 20px,
				transparent 20px 34px
			),
			repeating-linear-gradient(
				-64deg,
				rgba(4, 25, 48, 0.07) 0 1px,
				transparent 1px 15px,
				rgba(31, 127, 171, 0.08) 15px 17px,
				transparent 17px 31px
			);
		content: "";
		inset: 0;
		mix-blend-mode: multiply;
		opacity: 0.58;
		pointer-events: none;
		position: absolute;
		z-index: 0;
	}

	:global(html.dark) .deep-sea-surface::after {
		background:
			repeating-linear-gradient(
				116deg,
				rgba(255, 255, 255, 0.042) 0 2px,
				transparent 2px 18px,
				rgba(118, 217, 255, 0.028) 18px 20px,
				transparent 20px 34px
			),
			repeating-linear-gradient(
				-64deg,
				rgba(255, 255, 255, 0.018) 0 1px,
				transparent 1px 15px,
				rgba(56, 173, 223, 0.026) 15px 17px,
				transparent 17px 31px
			);
		mix-blend-mode: screen;
		opacity: 0.31;
	}
</style>
