---
import { marked } from "marked";

interface Props {
	of: string;
}

// https://github.com/astro-community/md/issues/20
const rendered = (await marked.parseInline(Astro.props.of)).replaceAll(
	"<a ",
	`<a rel="noreferrer" target="_blank"`,
);
---

<Fragment set:html={rendered} />
