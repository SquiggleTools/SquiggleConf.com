<section class="timeline-section" aria-label="Timeline preview">
	<slot />
</section>

<style>
	.timeline-section {
		--timeline-rail-background: light-dark(
			rgb(from white r g b / 42%),
			var(--colorBlueBright1)
		);
		--timeline-rail-border-color: light-dark(
			rgb(from var(--colorBlue4) r g b / 18%),
			var(--colorBlue3)
		);
		--timeline-cap-background: light-dark(
			rgb(from var(--colorBlueBright1) r g b / 62%),
			rgb(from var(--colorBlue2) r g b / 60%)
		);
		--timeline-cap-color: light-dark(
			var(--colorBlue5),
			rgb(from var(--colorWhiteIsh) r g b / 85%)
		);
		--timeline-time-color: light-dark(var(--colorBlue5), var(--colorWhiteIsh));
		--timeline-time-shadow: 0 1px 0
			light-dark(rgb(from white r g b / 46%), rgb(from white r g b / 22%));
		--timeline-venue-color: light-dark(
			rgb(from var(--colorBlue5) r g b / 90%),
			var(--colorHeading)
		);
		--timeline-venue-at-color: light-dark(
			rgb(from var(--colorBlue3) r g b / 68%),
			rgb(from var(--colorBlue2) r g b / 70%)
		);
		--timeline-venue-stack-shadow: 0 0.65rem 1.2rem
			light-dark(
				rgb(from var(--colorBlue5) r g b / 10%),
				rgb(from var(--colorBlue5) r g b / 18%)
			);
		--timeline-card-hover-background: linear-gradient(
			135deg,
			light-dark(
				rgb(from var(--colorBlue5) r g b / 5%),
				rgb(from white r g b / 13%)
			),
			light-dark(
				rgb(from var(--colorBlue5) r g b / 2%),
				rgb(from white r g b / 3%)
			)
		);
		--timeline-card-hover-shadow:
			inset 0 0 0 1px
				light-dark(
					rgb(from var(--colorBlue4) r g b / 10%),
					rgb(from white r g b / 8%)
				),
			0 1rem 2rem
				light-dark(
					rgb(from var(--colorBlue5) r g b / 8%),
					rgb(from var(--colorBlue5) r g b / 14%)
				);
		--timeline-card-glow-inner: light-dark(
			rgb(from var(--colorBlue4) r g b / 55%),
			color-mix(in srgb, var(--colorBlue2), white 8%)
		);
		--timeline-card-glow-outer: light-dark(
			rgb(from var(--colorBlue4) r g b / 25%),
			rgb(from var(--colorBlue2) r g b / 52%)
		);
		isolation: isolate;
		padding: 2rem clamp(0.15rem, 0.5vw, 1rem) clamp(3rem, 7vw, 5rem);
		position: relative;
	}
</style>
