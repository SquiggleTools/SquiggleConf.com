<span class="placeholder-mark" aria-hidden="true"> ? </span>

<style>
	.placeholder-mark {
		color: rgb(from white r g b / 92%);
		display: block;
		font-family: var(--fontFamilyHeading);
		font-size: 3rem;
		font-weight: 700;
		left: 50%;
		line-height: 1;
		pointer-events: none;
		position: absolute;
		text-shadow: 0 0.08rem 0.22rem rgb(from var(--colorBlue5) r g b / 30%);
		top: 50%;
		transform: translate(-50%, -50%);
		z-index: 3;
	}
</style>
